# use npmjs registry by default
registry=https://artifactory.srv.int.avast.com/artifactory/api/npm/npm

# reset auth related options if specified in ~/.npmrc file
_auth=""
always-auth=false

# use avast artifactory registry for packages in @avast scope
# see https://cml.avast.com/display/RE/Artifactory+as+a+npm+repository
# should work without setting auth info
# works only on internal network or VPN
@avast:registry=https://artifactory.srv.int.avast.com/artifactory/api/npm/npm